#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: the int to multiply with b
 * @b: the int to multiply with a
 * Return: 0 if success
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
